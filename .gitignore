node_modules/
dist/
out/
*.log

# Thư mục tạm phải trỏ sang ổ D khi build/cài (ổ C hết chỗ)
.build-tmp/
.install-tmp/

# Cấu hình cục bộ của máy (danh sách lệnh được phép) — không đẩy lên repo
.claude/settings.local.json
