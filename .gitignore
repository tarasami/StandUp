node_modules/
dist/
out/
*.log

# Thư mục tạm khi build/cài — trỏ TEMP sang đây nếu ổ hệ thống hết chỗ
.build-tmp/
.install-tmp/

# Cấu hình cục bộ của máy (danh sách lệnh được phép) — không đẩy lên repo
.claude/settings.local.json

# Hồ sơ chạy thử bản dev (tách khỏi bản đã cài) — chỉ có trên máy
.dev-profile/
